{
  "name": "todokid-native",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor Android wrapper for Tugas Harianku (native reminders)",
  "scripts": {
    "copyweb": "node scripts/copyweb.mjs",
    "sync": "npm run copyweb && cap sync android",
    "open": "cap open android",
    "build:apk": "npm run copyweb && cap sync android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor-firebase/authentication": "^6.3.0",
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/local-notifications": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
